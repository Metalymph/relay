# Backend Requirements

This document defines the conceptual requirements for Relay backends.

Backends may implement these concepts using different underlying technologies while preserving Relay semantics.