# Delivery Semantics

Relay provides reliability abstractions across multiple queue backends.

This document defines the delivery guarantees supported by Relay and how backend implementations map to them.